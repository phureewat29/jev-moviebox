# TypeSafe (Jev) - https://docs.typesafe.ai
TYPESAFE_API_KEY=
# The whole budget for one read, in milliseconds; past it the page shows an empty shelf.
TYPESAFE_TIMEOUT_MS=12000

# Optional. Serve the card and the shelf photo from a CDN; unset uses public/
NEXT_PUBLIC_CDN_URL=
